<% if logged_in? %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>惣菜レビューのSOUZAIへようこそ</h1>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>惣菜レビューのSOUZAIへようこそ</h1>
      <%= link_to '会員登録', signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>